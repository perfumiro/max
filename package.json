{
  "scripts": {
    "generate:catalog": "node scripts/generate-catalog.mjs",
    "typecheck": "node --check assets/admin/admin.js && node --check script.js",
    "lint": "node --check assets/admin/admin.js && node --check scripts/serve-local.cjs && node --check scripts/mobile-admin-audit.cjs",
    "build": "node scripts/generate-catalog.mjs",
    "test": "node scripts/mobile-admin-audit.cjs"
  },
  "dependencies": {
    "firebase": "^12.13.0"
  }
}
